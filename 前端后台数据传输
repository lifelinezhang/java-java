前端请求格式与对应的后端接受注解
一个http请求由三个部分构成：请求行， 请求头，请求体

请求行：
  1、请求方法
  2、资源对应的url地址，这个不是真正的url地址，需要和包头文的host属性组成完整的url请求
  3、协议名称及版本号
  
请求头：这里面有很多键值对，表示不同的信息，这里我只写我这能用到的
  1、Accept
  2、Content-type
Accept表示这次请求后台给我返回的数据格式，
Content-type表示这次请求，发送给后台的数据格式

当get请求时，不存在请求实体部分，也就无所谓数据格式。
所有数据在url中传给后台，用requestparam接受，路径参数用pathvariable接受。

当为post时，才有请求格式的问题，Content-type一般有这几种：
application/x-www-form-urlencoded        requestparam
非application/x-www-form-urlencoded      RequestBody